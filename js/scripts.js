var add = function(number1, number2) {
return number1 + number2;
};

var number1 = prompt(parseInt("Enter a number"));

var number2 = prompt(parseInt("Enter another number"));

alert(add(number1, number2));
